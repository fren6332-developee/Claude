# Dependencies
node_modules/

# Copied web assets (regenerated by `npm run copy:web` — don't duplicate the
# 9 MB of audio in git; it lives in ../mental-health-genes)
www/*
!www/.gitkeep

# Capacitor native projects are generated on your Mac with `npx cap add ios`.
# Commit them later if you want them versioned; ignored by default so the repo
# stays lean and platform-neutral.
ios/
android/

# Misc
.DS_Store
*.log
