# Per-job working state and raw/rendered media are local artifacts, not source.
projects/*
!projects/.gitkeep

outputs/*
!outputs/.gitkeep

# HyperFrames engine dependencies (npm install), not source.
engine/hyperframes/node_modules/

# WhisperX virtualenv (python3 -m venv .venv && pip install whisperx), not source.
.venv/
